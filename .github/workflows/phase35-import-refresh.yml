name: Phase 35 Import Refresh

on:
  push:
    branches:
      - phase35-device-profiles-proxmox

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase35-device-profiles-proxmox

      - uses: actions/setup-go@v5
        with:
          go-version-file: backend/go.mod

      - name: Format Phase 35 import files
        run: |
          gofmt -w             backend/internal/models/proxmox_source_state.go             backend/internal/proxmoximport/import.go             backend/internal/proxmoximport/import_test.go             backend/internal/gdb/proxmox_source_state.go             backend/internal/gdb/proxmox_script_import.go             backend/internal/gdb/proxmox_script_import_test.go             backend/internal/gdb/start.go             backend/internal/gdb/start_test.go             backend/internal/gdb/edit.go             backend/internal/gdb/export.go             backend/internal/backup/backup.go             backend/internal/api/api-proxmox-import.go             backend/internal/api/api_proxmox_import_test.go             backend/internal/api/api_export_test.go             backend/internal/api/routes.go

      - name: Refresh Swagger
        working-directory: backend
        run: go run github.com/swaggo/swag/cmd/swag@v1.16.6 init -g main.go --dir cmd/LANnventory,internal/api,internal/models,internal/proxmoximport,internal/proxmoxsnapshot

      - name: Test backend before committing
        working-directory: backend
        run: go test ./...

      - name: Commit formatted and generated state
        run: |
          if git diff --quiet; then
            echo "No generated or formatting changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/internal backend/docs
          git commit -m "phase35: refresh import generated state"
          git push origin HEAD:phase35-device-profiles-proxmox
